import Layout from '../Layout';
import { APP_NAME } from '../../config';
import './index.css';

const Home = () => {
  return (
    <Layout title='Home Page' description={APP_NAME}>
      ......
    </Layout>
  );
};

export default Home;
